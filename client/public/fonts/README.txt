Optional: place Sakkal Majalla font files here to bundle them for devices that do not have the font installed:
  majalla.ttf   (Regular)   — from C:\Windows\Fonts\majalla.ttf
  majallab.ttf  (Bold)      — from C:\Windows\Fonts\majallab.ttf
On Windows devices the font resolves locally and these files are not needed.
Note: Sakkal Majalla ships with Windows; confirm web-embedding rights before distributing the files (BRD GAP-13 / roadmap D5).
